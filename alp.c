#include<stdio.h>
int main()
{
char c;
printf("\n Enter the symbol");
scanf("%c",&c);
if((ch>=65&&ch<=90)||(ch>=97&&ch<=122))
{
printf("\n Enter character is alphabet");
}
else
{
printf("\nEnter character is not an alphabet");
}
return 0;
}
